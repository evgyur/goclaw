[Service]
# Loaded after the sealed runtime environment. The Haraldr broker may only
# override the entry hold; it cannot select signer, account, candidate, or mode.
EnvironmentFile=-/var/lib/trader20-v3/state/haraldr-control/entry-hold.env
